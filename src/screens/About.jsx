import { SectionWrapper } from '../wrapper';
import { motion } from 'framer-motion';

const About = () => {
  return <section className='relative w-full h-screen'>About</section>;
};

export default About;
